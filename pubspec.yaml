name: mobile_ecommerce
description: "Projecto para el ecommerce."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.1
  go_router: ^13.0.0
  cupertino_icons: ^1.0.6
  flutter_stripe: ^10.0.0
  speech_to_text: ^7.0.0
  flutter_tts: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
